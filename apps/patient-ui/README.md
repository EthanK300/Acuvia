# Patient UI Scaffold

This folder is intentionally scaffold-only.

Planned contents:

- `src/` for web portal source code
- `src/components/` for reusable UI components
- `src/pages/` for route-level views
- `src/services/` for API client calls to backend
- `public/` for static web assets

This client is intended to become the patient-facing web portal.
